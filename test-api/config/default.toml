[server]
host = "0.0.0.0"
port = 3000

[database]
url = "postgres://postgres:postgres@localhost/test-api"
max_connections = 10
min_connections = 2

[auth]
jwt_secret = "change-me-in-production"
jwt_expiration = 86400